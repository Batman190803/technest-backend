generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id           Int      @id @default(autoincrement())
  username     String   @unique
  passwordHash String
  role         String   @default("user")

  assetSnapshots AssetSnapshot[]
  documents      AssetDocument[]
  chatMessages   ChatMessage[]
}

model AssetSnapshot {
  id           Int      @id @default(autoincrement())
  user         User     @relation(fields: [userId], references: [id])
  userId       Int
  jsonPayload  String   // тут уже є
  createdAt    DateTime @default(now())
}

model AssetDocument {
  id        Int      @id @default(autoincrement())
  user      User     @relation(fields: [userId], references: [id])
  userId    Int

  assetId   String   // id активу з мобілки (AssetItem.id)
  fileName  String
  mimeType  String
  text      String?  // витягнутий текст PDF
  createdAt DateTime @default(now())
}

model ChatMessage {
  id        Int      @id @default(autoincrement())
  user      User     @relation(fields: [userId], references: [id])
  userId    Int

  role      String   // 'user' | 'assistant'
  content   String
  createdAt DateTime @default(now())
}
