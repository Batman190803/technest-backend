# 📝 Changelog - TechNest Backend

## [2.0.0] - 2025-12-08

### ✨ Основні покращення

#### 🤖 AI Помічник тепер читає PDF документи!
- **Проблема:** AI не міг надати інформацію з документів, бо текст не витягувався
- **Рішення:** Оновлено систему парсингу PDF з використанням pdf-parse v2
- **Результат:** AI тепер повноцінно аналізує технічну документацію

### 🆕 Додано

#### Нові API Endpoints:
1. `POST /api/documents/:documentId/reprocess` - переобробити окремий PDF документ
2. `POST /api/documents/reprocess-all` - переобробити всі PDF документи користувача
3. `GET /api/assets/:assetId/documents` - отримати всі документи активу
4. `GET /api/documents/:documentId` - отримати конкретний документ з текстом
5. `DELETE /api/documents/:documentId` - видалити документ

#### Покращення AI чату:
- Додано параметр `assetId` для фільтрації документів конкретного активу
- Збільшено ліміт документів з 5 до 10
- Збільшено preview тексту з 2000 до 3000 символів
- Додано контекст активів користувача
- Покращено системний промпт українською
- Додано метадані у відповіді (`documentsUsed`, `hasAssetContext`)

#### Покращення зберігання файлів:
- Постійне зберігання файлів у папці `/documents/`
- Унікальні імена файлів: `{timestamp}-{userId}-{assetId}-{originalName}`
- Обмеження розміру файлу: 10MB
- Фільтр типів файлів: PDF, JPEG, PNG, DOC, DOCX
- Автоматичне видалення файлів при видаленні документа/користувача

### 🔧 Виправлено

#### PDF парсинг:
- Оновлено `pdf-parse` до версії 2.x
- Додано `PDFParse` клас з правильною конфігурацією
- Покращена обробка помилок при парсингу
- Асинхронне читання файлів замість синхронного
- Детальні логи для діагностики

#### База даних:
- Додано поля `filePath` та `fileSize` до моделі `AssetDocument`
- Змінено тип поля `text` на `@db.Text` для довгих текстів
- Додано індекси для `userId` та `assetId`
- Додано каскадне видалення `onDelete: Cascade`

### 📦 Нові залежності

```json
{
  "pdf2json": "^4.0.0",
  "pdfjs-dist": "^5.4.449",
  "pdfkit": "^0.15.0"  // для тестування
}
```

### 🗄️ Зміни в базі даних

#### Модель `AssetDocument`:
```prisma
model AssetDocument {
  id        Int      @id @default(autoincrement())
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  assetId   String
  fileName  String
  mimeType  String
  filePath  String?   // ✅ Новое
  fileSize  Int?      // ✅ Новое
  text      String?   @db.Text  // ✅ Оновлено тип
  createdAt DateTime @default(now())

  @@index([userId])   // ✅ Новий індекс
  @@index([assetId])  // ✅ Новий індекс
}
```

### 📁 Нові файли

- `/documents/` - папка для постійного зберігання PDF
- `check-docs.js` - скрипт для перевірки документів у БД
- `reprocess-pdfs.js` - скрипт для переобробки існуючих PDF
- `test-extract.js` - тестовий скрипт для витягування тексту
- `ІНСТРУКЦІЯ_PDF.md` - детальна інструкція роботи з PDF
- `CHANGELOG.md` - цей файл

### 🔄 Оновлено

#### `.gitignore`:
```gitignore
# Uploaded documents and files
documents/
uploads/

# Database files
*.db
*.db-journal
```

### 🚀 Як оновитися

1. **Встановіть нові залежності:**
   ```bash
   npm install
   ```

2. **Примініть зміни БД:**
   ```bash
   npx prisma db push
   ```

3. **Переобробіть існуючі PDF:**
   ```bash
   # Через API
   curl -X POST https://your-api.com/api/documents/reprocess-all \
     -H "Authorization: Bearer YOUR_TOKEN"

   # Або через скрипт (на сервері)
   node reprocess-pdfs.js
   ```

4. **Перезапустіть сервер:**
   ```bash
   npm start
   ```

### 📊 Статистика змін

- **Файлів змінено:** 8
- **Файлів додано:** 6
- **Рядків коду додано:** ~500
- **Нових API endpoints:** 5
- **Покращень AI:** 7

### 🐛 Відомі обмеження

1. **Сканов���ні PDF** - текст не витягується, потрібен OCR
2. **Українське кодування** - деякі символи можуть відображатися некоректно в PDF генерованих PDFKit
3. **Розмір файлу** - обмеження 10MB на файл

### 🔮 Майбутні покращення

- [ ] Додати OCR для сканованих PDF (tesseract.js)
- [ ] Витягування таблиць з PDF
- [ ] Витягування зображень з PDF
- [ ] Генерація мініатюр сторінок
- [ ] Підтримка більше форматів (DOCX, XLSX)
- [ ] Пошук по тексту документів
- [ ] Версіонування документів

---

## [1.0.0] - Попередні версії

### Початкові функції:
- Автентифікація з 2FA
- Управління активами
- Завантаження документів (без парсингу)
- AI чат (без доступу до документів)
- Admin панель

---

**Примітка:** Для детальної інформації про роботу з PDF дивіться [ІНСТРУКЦІЯ_PDF.md](./ІНСТРУКЦІЯ_PDF.md)
