# ✅ Як виправити проблеми з AI

**Швидка інструкція для вирішення ваших проблем**

---

## 🎯 ПРОБЛЕМА: "AI каже 4 документи, а у мене 1"

### Чому так?
В базі даних дійсно є 4 документи. Ви завантажували документи кілька разів, але старі не видалили.

### Рішення

#### Варіант 1: Видалити локально (якщо маєте доступ до сервера)

```bash
cd technest-backend
node delete-old-docs.js
```

Скрипт видалить документи 1, 2, 3 і залишить тільки документ 4.

**Або** відредагуйте файл `delete-old-docs.js` і вкажіть які документи видалити:
```javascript
const DOCS_TO_DELETE = [1, 2, 3]; // Ваші ID
```

#### Варіант 2: Видалити через API (якщо код вже задеплоєно)

```bash
# Отримайте ваш JWT токен
TOKEN="ваш_токен_тут"

# Видаліть старі документи
curl -X DELETE https://technest-backend.onrender.com/api/documents/1 \
  -H "Authorization: Bearer $TOKEN"

curl -X DELETE https://technest-backend.onrender.com/api/documents/2 \
  -H "Authorization: Bearer $TOKEN"

curl -X DELETE https://technest-backend.onrender.com/api/documents/3 \
  -H "Authorization: Bearer $TOKEN"
```

#### Варіант 3: Видалити через мобільний додаток

Якщо в додатку є функція видалення документів - використайте її.

---

## 🎯 ПРОБЛЕМА: "AI не бачить активи, каже що є тільки категорії"

### Чому так?
В базі даних є 15 категорій, але **0 активів**. Активи не були створені через мобільний додаток.

### Перевірка

```bash
# Перевірте чи є активи в базі
node check-all-assets.js
```

Має показати список активів. Якщо список порожній - активи не створені.

### Рішення: Створити активи в мобільному додатку

1. **Відкрийте мобільний додаток**
2. **Перейдіть в розділ "Активи" або "Обладнання"**
3. **Створіть активи**, наприклад:
   - PC-001 (Комп'ютер Dell)
   - PC-004 (Ноутбук HP)
   - PC-007 (Робоча станція)
4. **Переконайтесь що вони збереглись**
5. **Перевірте синхронізацію з сервером**

### Після створення активів:

```bash
# Перевірте що активи з'явились в базі
node check-all-assets.js
```

Тепер AI зможе бачити і аналізувати ваше обладнання!

---

## 🚀 ПОВНА ІНСТРУКЦІЯ ВИПРАВЛЕННЯ

### Крок 1: Оновити код на сервері

```bash
cd technest-backend
git add .
git commit -m "fix: AI тепер показує що немає активів"
git push origin main
```

Дочекайтесь завершення деплою на Render.com.

### Крок 2: Видалити зайві документи

**Локально** (якщо є доступ):
```bash
node delete-old-docs.js
```

**Або через API** (після деплою):
```bash
TOKEN="ваш_токен"
curl -X DELETE https://technest-backend.onrender.com/api/documents/1 -H "Authorization: Bearer $TOKEN"
curl -X DELETE https://technest-backend.onrender.com/api/documents/2 -H "Authorization: Bearer $TOKEN"
curl -X DELETE https://technest-backend.onrender.com/api/documents/3 -H "Authorization: Bearer $TOKEN"
```

### Крок 3: Створити активи в додатку

1. Відкрийте мобільний додаток
2. Створіть активи обладнання
3. Перевірте що вони збереглись

### Крок 4: Протестувати AI

```bash
curl -X POST https://technest-backend.onrender.com/api/ai/chat \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Скільки у мене документів та активів?"}'
```

**Очікувана відповідь**:
```json
{
  "reply": "У вас:\n- 15 категорій обладнання\n- X одиниць обладнання\n- 1 завантажений документ\n- 1 документ з розпізнаним текстом",
  "stats": {
    "documentsTotal": 1,
    "documentsWithText": 1,
    "categoriesCount": 15,
    "assetsCount": X
  }
}
```

---

## 📊 ДІАГНОСТИКА

### Перевірити документи:
```bash
node check-docs.js
```

### Перевірити активи:
```bash
node check-all-assets.js
```

### Перевірити активи користувача:
```bash
node check-assets.js
```

---

## ✅ ЩО БУЛО ВИПРАВЛЕНО В КОДІ

1. **AI тепер чітко повідомляє коли немає активів**
   ```
   ⚠️ У вас ще немає створених активів обладнання.
   Створіть активи через мобільний додаток.
   ```

2. **Статистика показується завжди**
   - Навіть якщо активів 0
   - Навіть якщо документів 0

3. **Створені скрипти для діагностики та видалення**
   - `check-docs.js` - перевірка документів
   - `check-assets.js` - перевірка активів
   - `check-all-assets.js` - всі активи в БД
   - `delete-old-docs.js` - видалення старих документів

---

## 🆘 ЯКЩО ЩОСЬ НЕ ПРАЦЮЄ

1. **Перевірте логи на Render.com**
2. **Запустіть діагностичні скрипти**
3. **Перегляньте файл [ДІАГНОСТИКА.md](ДІАГНОСТИКА.md)**
4. **Перевірте чи код задеплоєно**

---

**Створено**: 2025-12-08
**Версія**: 1.0
